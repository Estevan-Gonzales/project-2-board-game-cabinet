<section class="card">
  <a href="/game-page/{{id}}">
    <header>{{names}}</header>

    <div class="card-body">
      <img src="{{image_url}}">
      <p>Description: {{description}}</p>
    </div>
  </a>
</section>