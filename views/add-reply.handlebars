<section class="card">
    <h3><a href="/user/{{id}}">{{poster}}'s</a> review of {{game_id}}</h3>

    <div class="card-body">
      <p>{{review_text}}</p>
     </div>
</section>

<section>
    <form>
        <label>Leave a Reply:</label>
        <input class="form-input"type="text">
        <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-full">
        Submit
        </button>
    </form>
</section>